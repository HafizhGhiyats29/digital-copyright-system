services:
  mongodb:
    image: mongo:7
    container_name: digital-copyright-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
    environment:
      MONGO_INITDB_DATABASE: digital_copyright
